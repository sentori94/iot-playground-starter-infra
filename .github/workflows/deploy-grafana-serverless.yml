name: Deploy Grafana Serverless (ECS)

on:
  workflow_dispatch:
    inputs:
      MODE:
        description: "Terraform mode"
        type: choice
        required: true
        default: plan
        options: [plan, apply]


env:
  AWS_REGION: eu-west-3
  TF_WORKING_DIR: ./infra/envs/grafana-serverless-dev

jobs:
  deploy-grafana:
    name: "Deploy Grafana [${{ github.event.inputs.MODE }}]"
    runs-on: ubuntu-latest

    env:
      AWS_REGION: eu-west-3
      TF_WORKING_DIR: ./infra/envs/grafana-serverless-dev

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Create S3 Backend + DynamoDB (if not exists)
        run: |
          set -e
          BUCKET="iot-playground-tfstate-grafana-serverless"
          REGION="${{ env.AWS_REGION }}"
          
          # S3 bucket (idempotent)
          if aws s3api head-bucket --bucket "$BUCKET" 2>/dev/null; then
            echo "✅ Bucket $BUCKET already exists"
          else
            aws s3api create-bucket --bucket "$BUCKET" \
              --region "$REGION" \
              --create-bucket-configuration LocationConstraint="$REGION"
            echo "✅ Created bucket $BUCKET"
          fi
          
          # Enable versioning
          aws s3api put-bucket-versioning \
            --bucket "$BUCKET" \
            --versioning-configuration Status=Enabled
          echo "✅ Versioning enabled"
          
          # Enable encryption
          aws s3api put-bucket-encryption \
            --bucket "$BUCKET" \
            --server-side-encryption-configuration '{
              "Rules": [{
                "ApplyServerSideEncryptionByDefault": {
                  "SSEAlgorithm": "AES256"
                }
              }]
            }'
          echo "✅ Encryption enabled"
          
          # Block public access
          aws s3api put-public-access-block \
            --bucket "$BUCKET" \
            --public-access-block-configuration \
              BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true
          echo "✅ Public access blocked"
          
          # DynamoDB table (idempotent)
          if aws dynamodb describe-table --table-name terraform-lock-grafana-serverless --region "$REGION" >/dev/null 2>&1; then
            echo "✅ DynamoDB table terraform-lock-grafana-serverless already exists"
          else
            aws dynamodb create-table \
              --table-name terraform-lock-grafana-serverless \
              --region "$REGION" \
              --billing-mode PAY_PER_REQUEST \
              --attribute-definitions AttributeName=LockID,AttributeType=S \
              --key-schema AttributeName=LockID,KeyType=HASH
            echo "✅ Created DynamoDB table terraform-lock-grafana-serverless"
          fi

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0

      - name: Terraform Init
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: terraform init

      - name: Terraform Validate
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: terraform validate

      - name: Terraform Plan
        if: github.event.inputs.MODE == 'plan'
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: terraform plan

      - name: Terraform Apply
        if: github.event.inputs.MODE == 'apply'
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: |
          echo "🚀 Deploying Grafana Serverless infrastructure..."
          terraform apply -auto-approve


      - name: Show Grafana Outputs
        if: github.event.inputs.MODE == 'apply'
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: |
          echo "🎉 Grafana Serverless deployed successfully!"
          echo ""
          echo "📋 Grafana Outputs:"
          echo ""
          GRAFANA_URL=$(terraform output -raw grafana_url 2>/dev/null || echo "Not available yet")
          
          echo "  🌐 Grafana URL: $GRAFANA_URL"
          echo ""
          echo "📝 Prochaines étapes:"
          echo "  1. Accéder à Grafana: $GRAFANA_URL"
          echo "  2. Login: admin / [votre mot de passe]"
          echo "  3. Dashboard disponible: IoT Serverless - CloudWatch Monitoring"
          echo ""
          echo "ℹ️  CloudWatch Logs et Metrics sont automatiques (pas de configuration)"
          echo "ℹ️  Les logs apparaîtront dès la première requête Lambda"

      - name: Plan Summary
        if: github.event.inputs.MODE == 'plan'
        run: |
          echo "📋 Terraform plan completed!"
          echo "To apply these changes, run this workflow again with MODE=apply"

